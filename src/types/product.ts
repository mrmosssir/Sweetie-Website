export type Product = {
  title: string;
  category: string;
  origin_price: string | number;
  price: string | number;
  unit: string;
  description: string;
  content: string;
  is_enabled: boolean;
  imageUrl: string;
};
