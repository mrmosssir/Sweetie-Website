export type ApiAdminCategory = {
  id: string;
  name: string;
  image: string;
  is_enabled: boolean;
};

export type AdminCategory = {
  id: string;
  name: string;
  image: string;
  isEnabled: boolean;
};

export type Category = {
  id: string;
  name: string;
  image: string;
};
